apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: minio
  namespace: web-system
  annotations:
    kubernetes.io/ingress.class: nginx
spec:
  rules:
  - host: "minio.happylittlecloud.ca"
    http:
      paths:
      - path: /
        pathType: Prefix
        backend:
          service:
            name: minio
            port:
              number: 80
